site_name: Estrutura Workshop ao vivo
theme:
  name: material # or other theme
  palette:
    primary: blue
  language: pt

repo_name: "wendersonfag/estrutura_workshop"
repo_url: "https://github.com/wendersonfag/estrutura_workshop.git"

markdown_extensions:
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid

plugins:
  - search
  - mkdocstrings:
      default_handler: python
